#updates such as Item drain feeding

#run again on failed conditions (check if all the items reusest reduced to 0)

#once all conditions pass call end function
function world_boarder_shrine:main-1/end